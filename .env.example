# Mapbox public access token (pk.*) — get one at https://account.mapbox.com/
# Used at runtime by the map screen. Without it, the map renders a fallback message.
EXPO_PUBLIC_MAPBOX_TOKEN=

# Supabase (added later, currently unused by the map MVP)
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
